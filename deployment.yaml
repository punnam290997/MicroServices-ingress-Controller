apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-app
  labels:
    app: ingress-app

spec:
  replicas: 3
  selector:
    matchLabels:
      app: ingress-app

  template:
    metadata:
      labels:
        app: ingress-app

    spec:
      containers:
      - name: deployment-ingress-container
        image: punnam2909/image:latest
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: deployment-service

spec:
  type: ClusterIP
  selector:
    app: ingress-app

  ports:
  - port: 80
    targetPort: 80
